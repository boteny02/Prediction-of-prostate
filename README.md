Task
Implement a medical image classification pipeline using the "MRI_Metadata.xlsx" file and corresponding MRI images. The pipeline involve feature extraction with a pre-trained ResNet model, feature selection using the ReliefF algorithm, training a suitable classifier, and evaluating its performance.
